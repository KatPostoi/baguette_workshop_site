export type MaterialItem = {
  id: string;
  title: string;
  material: string;
  description: string;
  image: {
    src: string;
    alt: string;
  };
};

export type StylelItem = {
  id: string;
  style: string;
};
